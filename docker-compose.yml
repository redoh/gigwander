services:
  postgres:
    image: postgres:16-alpine
    container_name: gigwander-db
    environment:
      POSTGRES_USER: gigwander
      POSTGRES_PASSWORD: gigwander
      POSTGRES_DB: gigwander
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
